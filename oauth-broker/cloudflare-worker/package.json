{
  "name": "direct-xfer-public-oauth-broker",
  "version": "1.71.19",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 migrations apply direct-xfer-oauth-broker --remote"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
